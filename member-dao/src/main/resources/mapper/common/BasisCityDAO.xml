<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gt.member.dao.common.BasisCityDAO">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.gt.common.entity.BasisCity">
		<id column="id" property="id" />
		<result column="city_code" property="cityCode" />
		<result column="city_name" property="cityName" />
		<result column="city_parent" property="cityParent" />
		<result column="city_level" property="cityLevel" />
	</resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, city_code, city_name, city_parent, city_level
    </sql>

	<select id="findBasisCity" resultType="Map">
		SELECT id,city_code,city_name FROM basis_city t WHERE city_level=2
	</select>

	<select id="findBaseisCityByCode" resultType="Map">
		SELECT * FROM basis_city WHERE city_parent IN (SELECT id FROM basis_city t WHERE city_code=#{cityCode} )

	</select>

</mapper>
