<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gt.member.dao.common.MsgTemplateDAO">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.gt.common.entity.MsgTemplate">
		<id column="id" property="id" />
		<result column="template_id" property="templateId" />
		<result column="title" property="title" />
		<result column="primary_industry" property="primaryIndustry" />
		<result column="deputy_industry" property="deputyIndustry" />
		<result column="content" property="content" />
		<result column="example" property="example" />
		<result column="status" property="status" />
		<result column="bus_id" property="busId" />
		<result column="fileds" property="fileds" />
		<result column="filed_size" property="filedSize" />
	</resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, template_id, title, primary_industry, deputy_industry, content, example, status, bus_id, fileds, filed_size
    </sql>

	<select id="selectTempObjByBusId"   resultType="map"  parameterType="map">
		select id,title
		from  t_wx_msg_template  t
		where 	t.bus_id=#{busId} and t.status !=-1
	</select>

</mapper>
