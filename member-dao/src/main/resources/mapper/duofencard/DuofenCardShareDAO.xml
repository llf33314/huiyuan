<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gt.member.dao.duofencard.DuofenCardShareDAO">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.gt.duofencard.entity.DuofenCardShare">
		<id column="id" property="id" />
		<result column="getId" property="getId" />
		<result column="memberId" property="memberId"/>
	</resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, getId AS getId,memberId as memberId
    </sql>

	
	<select id="findDuofenCardShareByMemeberId" resultType="Map">
		select getId from t_duofen_card_share where memberId=#{memberId}

	</select>
	
</mapper>
